'use client';
// Lucide is the system's only icon set. The design system's own <Icon> wrapper
// expects a UMD global, so the site uses lucide-react directly; re-exporting
// through this client barrel keeps icons usable from server components too.
export {
  ArrowLeft,
  ArrowRight,
  ArrowUpRight,
  CalendarDays,
  Check,
  ChevronDown,
  Clock,
  ExternalLink,
  Mail,
  MapPin,
  Menu,
  PenLine,
  Send,
  User,
  X,
} from 'lucide-react';
