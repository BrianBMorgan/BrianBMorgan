Icon-only square control for toolbars, card actions and nav. Always pass `label` for accessibility.

```jsx
<IconButton label="GitHub" variant="secondary"><Icon name="github" size={18} /></IconButton>
<IconButton label="Menu" round><Icon name="menu" /></IconButton>
```
